# 测试环境配置

# 多个 base_url 配置
# NOTE: 同一环境中不同接口可能需要不同的 base_url
# 用法: 测试用例中必须指定 base_url 别名，如 base_url: "operation"
base_urls:
  # 新运营系统
  operation: "https://dev-aly-us-ad-web.cdcicd.com"
  # siteId
  siteId: "http://test-api.chongdaads.com"
  # DSP
  dsp: "http://test-huiinex-dsp-web.cdcicd.com"

# 认证配置
auth:
  # 登录重试配置
  retry:
    max_retries: 3          # 最大重试次数
    retry_delay: 2          # 重试间隔（秒）
    timeout: 30             # 登录超时时间（秒）

# 默认请求头
headers:
  Content-Type: "application/json"
  Accept: "application/json"
  User-Agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/144.0.0.0 Safari/537.36"

# 默认请求参数
params:
  debug: "false"

# 超时配置
timeout:
  connect: 10
  read: 30

# 数据库配置
database:
  host: "rm-j6c68qtq22l5p8yciso.mysql.cnhk.rds.aliyuncs.com"
  port: 3306
  user: "adnet"
  password: "JGXuY8z4NedHrDJ"
  database: "adnetwork-server"
