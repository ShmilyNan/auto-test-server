# 测试环境配置

# 基础URL
# NOTE: 用于构建完整的请求URL，作为默认 base_url
base_url: "https://dev-aly-us-ad-web.cdcicd.com"

# 多个 base_url 配置
# NOTE: 同一环境中不同接口可能需要不同的 base_url
# 用法: 测试用例中指定 base_url 别名，如 base_url: "admin"
base_urls:
  # 默认 API 服务
  default: "https://dev-aly-us-ad-web.cdcicd.com"
  # siteId API
  siteId: "http://test-api.chongdaads.com"

# 代理配置
proxy:
  enable: false
  http: ""
  https: ""

# 认证配置
auth:
  username: "Lee"
  password: "Working@1130"

# 默认请求头
headers:
  Content-Type: "application/json"
  Accept: "application/json"
  User-Agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/144.0.0.0 Safari/537.36"

# 默认请求参数
#params:
#  debug: "false"

# 超时配置
timeout:
  connect: 10
  read: 30

# 数据库配置
database:
  host: "rm-j6c68qtq22l5p8yciso.mysql.cnhk.rds.aliyuncs.com"
  port: 3306
  user: "adnet"
  password: "JGXuY8z4NedHrDJ"
  database: "adnetwork-server"
